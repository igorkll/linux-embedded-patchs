diff --git a/kernel/printk/printk.c b/kernel/printk/printk.c
index 57eac02ea..cc2d65b58 100755
--- a/kernel/printk/printk.c
+++ b/kernel/printk/printk.c
@@ -2378,14 +2378,14 @@ EXPORT_SYMBOL_GPL(vprintk_default);
 
 asmlinkage __visible int _printk(const char *fmt, ...)
 {
-	va_list args;
-	int r;
-
-	va_start(args, fmt);
-	r = vprintk(fmt, args);
-	va_end(args);
+	//va_list args;
+	//int r;
+	//
+	//va_start(args, fmt);
+	//r = vprintk(fmt, args);
+	//va_end(args);
 
-	return r;
+	return 0;
 }
 EXPORT_SYMBOL(_printk);
 
@@ -2412,18 +2412,18 @@ struct console *early_console;
 
 asmlinkage __visible void early_printk(const char *fmt, ...)
 {
-	va_list ap;
-	char buf[512];
-	int n;
-
-	if (!early_console)
-		return;
-
-	va_start(ap, fmt);
-	n = vscnprintf(buf, sizeof(buf), fmt, ap);
-	va_end(ap);
-
-	early_console->write(early_console, buf, n);
+	//va_list ap;
+	//char buf[512];
+	//int n;
+	//
+	//if (!early_console)
+	//	return;
+	//
+	//va_start(ap, fmt);
+	//n = vscnprintf(buf, sizeof(buf), fmt, ap);
+	//va_end(ap);
+	//
+	//early_console->write(early_console, buf, n);
 }
 #endif
 
